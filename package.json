{
  "name": "v2r-api",
  "version": "1.0.0",
  "description": "Secure API backend for V2R Builds",
  "main": "index.js",
  "scripts": {
    "dev": "vercel dev",
    "build": "mkdir -p public && cp *.html *.css *.js public/ 2>/dev/null || : && cp -r images videos public/ 2>/dev/null || :"
  },
  "dependencies": {
    "form-data": "^4.0.0",
    "node-fetch": "^2.6.7",
    "replicate": "^1.4.0"
  }
}
